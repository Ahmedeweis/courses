{
  "name": "tailwind-project",
  "version": "1.0.0",
"scripts": {
  "build": "mkdir -p public && npx tailwindcss -i ./src/input.css -o ./public/output.css --minify && cp src/*.html public/ && cp -r src/data public/ && cp -r src/img public/"
},
  "devDependencies": {
    "autoprefixer": "^10.0.0",
    "postcss": "^8.0.0",
    "tailwindcss": "^3.0.0"
  }
}